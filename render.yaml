services:
  - type: web
    name: jobadmin-backend
    runtime: dotnet
    plan: free
    rootDir: JobManagementAPI
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/JobManagementAPI.dll
